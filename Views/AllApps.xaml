<UserControl x:Class="ProcessManager.Views.AllApps"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProcessManager.Views"
      xmlns:hc="https://handyorg.github.io/handycontrol"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <WrapPanel Height="50">
                <Grid Width="20"></Grid>
                <Button Height="40" Content="折线图" FontSize="20"></Button>
                <Grid Width="60"></Grid>
                <Button Height="40" Content="扇形图" FontSize="20"></Button>
 
                <Grid Width="140"></Grid>
                <Button Height="40" Content="确认" FontSize="20"></Button>
            </WrapPanel>
        <DataGrid Height="500" hc:DataGridAttach.CanUnselectAllWithBlankArea="True" HeadersVisibility="All" RowHeaderWidth="50" AutoGenerateColumns="False" ItemsSource="{Binding DataList}">
            <DataGrid.RowHeaderTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected,RelativeSource={RelativeSource AncestorType=DataGridRow}}"/>
                </DataTemplate>
            </DataGrid.RowHeaderTemplate>
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Width="60" CanUserResize="False" Binding="{Binding Index}" Header="index"/>
                <DataGridTemplateColumn Width="60" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImgPath}" MaxWidth="32" MaxHeight="32" Stretch="Uniform"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding Name}" Header="name"/>
                <DataGridCheckBoxColumn Width="60" CanUserResize="False" Binding="{Binding IsSelected}" Header="check"/>
                <DataGridComboBoxColumn  Width="100" CanUserResize="False" SelectedValueBinding="{Binding Type}" Header="type"/>
                <DataGridTextColumn Binding="{Binding Remark}" Header="remark"/>
            </DataGrid.Columns>
        </DataGrid>
            
            <WrapPanel Height="50">
                <Grid Width="20"></Grid>
                <Button Height="40" Content="主页" FontSize="20" Click="HomePageClick"></Button>
                <Grid Width="20"></Grid>
                <Button Height="40" Content="单个应用" FontSize="20" Click="SingleAppClick"></Button>
                <Grid Width="20"></Grid>
                <Button Height="40" Content="监视" FontSize="20" Click="MonitorClick"></Button>
                <Grid Width="20"></Grid>
                <Button Height="40" Content="设置" FontSize="20"></Button>
            </WrapPanel>
        </StackPanel>
        <Frame Grid.Column="1" Source="AllAppsRight2.xaml" ></Frame>
    </Grid>
</UserControl>
