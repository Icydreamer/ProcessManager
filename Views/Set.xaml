<UserControl x:Class="MvvmTutorials.ToolkitMessages.Views.Set"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MvvmTutorials.ToolkitMessages.Views"
                   xmlns:hc="https://handyorg.github.io/handycontrol"
      mc:Ignorable="d" 
     Height="600" Width="1000"
   >

    <Grid Margin="10,20,0,0" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="150, 50, 100, 50">
            <hc:SimpleText Text="设置" FontSize="30" TextAlignment="Center" Height="50" Width="166" HorizontalAlignment="Center"/>
            <Grid Height="40"/>
            <DockPanel>
                <TextBlock Text="开机自启" FontSize="20" Foreground="{DynamicResource TEXTCOLOR}" Height="25" VerticalAlignment="Center" />
                <ToggleButton IsChecked="False" Margin="5,9" Style="{StaticResource ToggleButtonSwitch}" HorizontalAlignment="Right" />
            </DockPanel>
            <!--<Grid Height="10"/>-->
            <!--DockPanel>
                <TextBlock Text="后台运行" FontSize="20" Foreground="{DynamicResource TEXTCOLOR}" Height="25" VerticalAlignment="Center"/>
                <ToggleButton IsChecked="False" Margin="5,9" Style="{StaticResource ToggleButtonSwitch}" HorizontalAlignment="Right" />
            </DockPanel>-->
            <Grid Height="10"/>
            <DockPanel>
                <TextBlock Text="桌面提醒" FontSize="20" Foreground="{DynamicResource TEXTCOLOR}" Height="25" VerticalAlignment="Center"/>
                <ToggleButton IsChecked="False" Margin="5,9" Style="{StaticResource ToggleButtonSwitch}" HorizontalAlignment="Right" />
            </DockPanel>
            <Grid Height="10"/>
            <DockPanel>
                <TextBlock Text="深色模式" FontSize="20" Foreground="{DynamicResource TEXTCOLOR}" Height="25" VerticalAlignment="Center"/>
                <ToggleButton IsChecked="False" Margin="5,9"  Style="{StaticResource ToggleButtonSwitch}" HorizontalAlignment="Right" hc:VisualElement.HighlightBrush="{DynamicResource ReverseTextBrush}" Click="ToggleButton_Click"/>
            </DockPanel>
            <Grid Height="20"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="50">
            <Grid Height="125"/>
            <Button Content="导出csv文件" Height="40" FontSize="20" HorizontalAlignment="Left" Click="csv"  Background="{DynamicResource BUTTONCOLOR}" Foreground="{DynamicResource TEXTCOLOR}"/>
            <Grid Height="20"/>
            <Button Content="清除缓存" Height="40" FontSize="20" HorizontalAlignment="Left" Click="dataremove"  Background="{DynamicResource BUTTONCOLOR}" Foreground="{DynamicResource TEXTCOLOR}"/>
            <Grid Height="50"/>
            <WrapPanel HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</UserControl>
